```rust
let x = &mut a[1];
let y = &a[2]; // ❌ ERROR         Rust treats all a[_] accesses as one "place".
```
```rust
let (left, right) = a.split_at_mut(2);
let x = &mut left[1];
let y = &right[0];
```
```rust
fn return_a_string(output: &mut String) {
    output.replace_range(.., "Hello"); // caller-provided mutable reference
}

fn stringify_name_with_title(name: &Vec<String>) -> String {
let mut full = name.join(" "); // Avoid mutation altogether. Do a transformation first, that only needs reference/&self or clone it altogeter.
full.push_str(" Esq.");
}
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
```rust
```
